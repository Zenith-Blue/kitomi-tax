# レスポンシブ対応 指示書

きとみ税理士法人 公式サイト

このドキュメントは、PC版が完成したサイトに対してレスポンシブ対応（タブレット・スマートフォン表示の最適化）を行うための指示書です。Claude Code はこのファイルを参照し、以下の基準に沿って実装・修正を行ってください。

## 1. 前提・ゴール

- 既存の PC 版デザイン・レイアウトは原則変更しない。タブレット・スマホ表示でも崩れず、読みやすく、操作しやすい状態にすることがゴール。
- 想定される閲覧者は、税理士法人を探している経営者・個人事業主・一般の方。**スマートフォンからの閲覧比率が高い**ことを前提に、スマホでの可読性・操作性を最優先で確保する。
- 信頼感のある士業サイトとしての品格を、どの画面幅でも維持する（崩れ・はみ出し・極端な詰まりが見えないこと）。

## 2. ブレイクポイント

判断は Claude Code に任せるが、特段の理由がない限り以下を基準にすること。

| デバイス | 画面幅の目安 |
| :-: | :-: |
| スマートフォン | 〜767px |
| タブレット | 768px〜1023px |
| PC（既存デザイン） | 1024px以上 |

- 既存コードが Tailwind CSS / Bootstrap など特定のフレームワークを使用している場合は、そのフレームワークの標準ブレイクポイントに合わせてよい。
- メディアクエリは原則 **モバイルファースト**（`min-width` ベース）で記述する。既存実装が `max-width` ベースの場合はそれに合わせて統一する。

## 3. レイアウトの原則

1. **横スクロールを絶対に発生させない。** どの画面幅でも `body` に水平スクロールバーが出ないこと。
2. **多段組（2カラム以上）は、タブレット以下で縦積みに切り替える。** ただし、明らかに横並びのまま成立する小要素（アイコン＋テキストなど）は除く。
3. **コンテナの最大幅は維持し、左右に最低 16px の余白を確保する。** スマホで文字や画像が画面端にぴったり張り付かないこと。
4. **PC で設定された `padding` / `margin` をそのまま縮小しない。** スマホでは縦方向の余白を 60〜75% 程度に、横方向は 16〜24px に再調整する。
5. **`width` の固定値（px）は可能な限り `%` / `rem` / `max-width` に置き換える。** 画像・カード・ボタンが画面幅からはみ出さないこと。

## 4. タイポグラフィ

- 本文の最小サイズは **16px**（スマホでも縮めない）。
- 見出し（h1〜h3）はスマホで PC の 70〜85% 程度に縮小してよいが、`h1` で 24px、`h2` で 20px を下回らない。
- 行間（line-height）は本文 1.6〜1.8 を確保する。詰まって見える場合は広げる。
- 長い英数字・URL・メールアドレスは `word-break: break-word;` または `overflow-wrap: anywhere;` を指定し、画面外にはみ出さないようにする。

## 5. 画像・メディア

- すべての `<img>` に `max-width: 100%; height: auto;` を適用する。
- 装飾画像はスマホで表示しない or 縮小してもよい（CLS が起きないよう width / height 属性は残す）。
- 背景画像（background-image）は、スマホでは `background-size: cover;` の挙動を再確認し、人物・ロゴが見切れないよう `background-position` を調整する。
- 動画・iframe（Google Map など）は `aspect-ratio` プロパティ、または親要素に padding-top トリックを使って縦横比を維持する。

## 6. ナビゲーション

- PC：既存のグローバルナビ（横並び）を維持。
- タブレット以下：**ハンバーガーメニュー**に切り替える。
  - 開閉アニメーションは 200〜300ms 程度で違和感のない速度にする。
  - メニュー展開中は背景をスクロール禁止にする（`overflow: hidden` を body に付与）。
  - 閉じるボタン（×）またはメニュー外タップで閉じられるようにする。
- ロゴはスマホで縮小し、ハンバーガーアイコンと重ならないようにする。

## 7. フォーム（お問い合わせ等）

- 入力欄はスマホで **必ず縦積み**にする。横並びにしない。
- 入力欄の高さは **最低 44px**、文字サイズは **16px 以上**（iOS で自動ズームが発生しないようにするため）。
- 送信ボタンは画面幅いっぱい（または最低 200px 幅）にし、誤タップしにくいサイズにする。
- ラベルは入力欄の上に配置する（プレースホルダーのみに頼らない）。
- エラーメッセージは赤色＋アイコンで明示する。

## 8. テーブル（料金表など、もし存在する場合）

- スマホでは以下のいずれかの方式に切り替える：
  - 方式 A：横スクロール（`overflow-x: auto;` を親要素に付与、スクロール可能であることを視覚的に示す）
  - 方式 B：カード型レイアウト（1行 = 1カード として縦積み）
- 表全体を縮小して読めなくするのは NG。

## 9. タッチターゲット・操作性

- すべてのリンク・ボタンは **最低 44 × 44px** のタップ領域を確保する。
- ホバー（`:hover`）でのみ表示される情報は、タブレット以下でも見えるようにする（タップで開く、常時表示、など）。
- 隣接するリンク同士の間隔は最低 8px 空ける。

## 10. パフォーマンス

- スマホ向けには、可能であれば画像を WebP 形式 or 適切なサイズに最適化する。
- `loading="lazy"` を装飾画像・ファーストビュー外の画像に付与する。
- 不要な大きい画像をスマホで読み込ませない（`<picture>` タグ or `srcset` の活用を検討）。

## 11. NG 事項（避けるべきこと）

- 横スクロールの発生
- 文字が画像化されていて、スマホで読めなくなる
- ボタン・リンクが小さすぎてタップできない
- フォームの入力欄が画面外にはみ出す
- ナビが PC のまま表示され、文字が重なる
- ファーストビューに収まるべきメインビジュアルが、スマホで見切れて意味が伝わらない

## 12. 実装後の確認チェックリスト

実装が完了したら、以下を Claude Code が自己チェックし、結果を報告すること。

- 320px、375px、414px、768px、1024px、1440px の各幅で表示崩れがないか
- iPhone Safari / Android Chrome / iPad / PC Chrome / PC Safari で挙動を確認したか（実機 or DevTools のデバイスモード）
- 横スクロールバーが出ていないか（全ページ）
- ハンバーガーメニューが正しく開閉し、メニュー外タップで閉じるか
- フォーム送信が各デバイスで正常に動くか
- Google PageSpeed Insights / Lighthouse の「モバイル」スコアが PC 版から大幅に落ちていないか
- 既存の PC 版デザイン（1024px 以上）が変わっていないか

## 13. 作業の進め方

1. まず現状の CSS 構造を把握し、既存の命名規則・設計（BEM / utility-first など）に合わせる。
2. ページ単位ではなく、**コンポーネント単位**で対応する（ヘッダー → ナビ → ヒーロー → 各セクション → フッター、の順）。
3. 各コンポーネントの修正後、スマホ・タブレット・PC の 3 サイズで目視確認してから次に進む。
4. コミットはコンポーネント単位で分けて、レビューしやすくする。
   - コミットメッセージ例：`feat(responsive): ヘッダーをスマホ対応`
5. 作業完了後、PR の説明欄に「12. 実装後の確認チェックリスト」の結果を貼り付ける。

## 14. 不明点が出たときの対応

- デザイン上の判断（色・余白・配置）で迷ったら、**既存 PC 版のテイストを最優先で維持**し、士業サイトとしての落ち着いた印象を崩さないこと。
- 仕様として判断できない部分（例：スマホで非表示にすべき要素があるか等）は、勝手に削除せず、コメント or Issue に残して相談すること。
